int main (void)
{
    printf("Hola Mundo");
    // git clone <repo>
    // git fetch (una vez clonado el repositorio, descarga la informacion)
    // git pull origin main (jala el commit al local)
    // git commit -am (guardar de manera local)
    // git push origin main (empujar los cambios al servidor remoto)
    // git reset --hard (volver al estado en el que estaba al comienzo)

    // git branch (nombre de la nueva linea) crear linea
    // git checkout (nombre de la linea)
    // git push origin (nombre del branch) (empujar los cambios Branch)
    // git checkout (nombre de donde se quiere posicionarse)

    //Otra frma de crear un BRANCH
    // git checkout -B (+nombre del la nueva linea)(crea el branch y se posiciona en él)

    //crear una nueva linea o branches o una rama, con la misma historia (commit) del main principal
    //Ambos trabajan en el mismo main y con la misma historia, pero en diferentes lienas 
    // para eso se utiliza "git branch y el nombre de la nueva linea que se despliega del main"
    // despues de creada la linea, para moverse se utiliza un nuevo comando

    //JUNTAR BRANCH CON EL MAIN
    // Pull Request (en Github)
    // despues que aceptan el pull request
    //Merge Pull Request - se confirma 


    /*Cambio 1 del MAIN*/

} 

